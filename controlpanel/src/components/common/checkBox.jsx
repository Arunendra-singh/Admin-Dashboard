// import React, { useState, useCallback } from "react";

// const CheckboxComponent = ({ type, data, index, checked, filterChangeHandler }) => {
//     return (
//         <div className="checkbox">
//             <input type="checkbox" data-index={index} checked={checked} onChange={(e) => filterChangeHandler(type, data.guid, e.target.checked)} />
//         </div>
//     );
// };
