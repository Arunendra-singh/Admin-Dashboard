import createContextStore from "common/hooks/react/createContextStore";

const Store = createContextStore({
    resources: null,
    takeatourdata: null,
    globalsetting: null
});

export default Store;
